/** Width of each rod cell (hole slot) in pixels */
export const CELL_SIZE = 48;

/** Rod height in pixels */
export const ROD_HEIGHT = 56;

/** Hole radius inside rods */
export const HOLE_RADIUS = 16;

/** Chip size (full-size on canvas) */
export const CHIP_SIZE = 42;

/** Chip size in bottom toolbar palette */
export const CHIP_PALETTE_SIZE = 26;

/** Snap distance for rod alignment */
export const SNAP_DISTANCE = 16;

/** Snap distance for chip-to-hole snapping */
export const CHIP_SNAP_DISTANCE = 24;

/** Trash zone height at canvas bottom */
export const TRASH_ZONE_HEIGHT = 60;

/** Frame cell size */
export const FRAME_CELL_SIZE = 50;

/** Frame inner padding */
export const FRAME_PADDING = 6;

/** Frame hole radius */
export const FRAME_HOLE_RADIUS = 17;

/** Mini rod unit width (sidebar preview) */
export const MINI_UNIT_WIDTH = 17;

/** Mini rod height (sidebar preview) */
export const MINI_ROD_HEIGHT = 22;

/** Maximum undo history entries */
export const MAX_HISTORY = 40;
